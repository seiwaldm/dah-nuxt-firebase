{
  "name": "dah-nuxt-firebase",
  "version": "1.0.0",
  "description": "Example project showcasing the use of @nuxtjs/firebase together with vuexfire.",
  "author": "Pascal Luther",
  "private": true,
  "scripts": {
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint",
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "npm run generate && firebase deploy --only hosting --project dah-nuxt-firebase-3345c"
  },
  "dependencies": {
    "@nuxtjs/firebase": "^7.3.0",
    "@nuxtjs/vuetify": "^1.12.3",
    "core-js": "^2.6.12",
    "firebase": "^8.2.1",
    "nuxt": "^2.12.2",
    "vuexfire": "^3.2.5"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.12.3",
    "nodemon": "^2.0.1",
    "prettier": "^2.0.4"
  }
}
